# Shared Directory
This directory is intended for code that will be shared between the client and the server.
Examples include validation schemas, constants, or type definitions (if migrating to TypeScript later).
